#include <stdio.h>

/**
 * main - This is Entry point
 *
 * return a zero (means success) and end program
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
